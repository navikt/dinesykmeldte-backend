name: Deploy dinesykmeldte-kafka app to dev
on:
  push:
    branches:
      - '**'
      - '!master'
    paths:
      - 'dinesykmeldte-kafka/**'
      - '.github/workflows/kafka-dev.yml'
      - 'build.gradle.kts'

jobs:
  snyk-test:
    name: Checkout code and analyze dependencies
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run Snyk to check for vulnerabilities
        uses: snyk/actions/gradle-jdk17@master
        env:
          ORG_GRADLE_PROJECT_githubUser: x-access-token
          ORG_GRADLE_PROJECT_githubPassword: ${{ secrets.GITHUB_TOKEN }}
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: test
          args: --org=teamsykmelding --severity-threshold=high
  build-kafka:
    permissions:
      contents: "read"
      id-token: "write"
    name: Checkout code and create docker tag
    runs-on: ubuntu-latest
    steps:
      - name: Setup java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Gradle wrapper validation
        uses: gradle/wrapper-validation-action@v1.0.5
      - name: Setup gradle dependency cache
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/.*gradle*') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Run tests
        env:
          ORG_GRADLE_PROJECT_githubUser: x-access-token
          ORG_GRADLE_PROJECT_githubPassword: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./gradlew :dinesykmeldte-kafka:test
      - name: Build jar
        env:
          ORG_GRADLE_PROJECT_githubUser: x-access-token
          ORG_GRADLE_PROJECT_githubPassword: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./gradlew :dinesykmeldte-kafka:shadowJar -x test
      - name: kafka-docker-build-push
        uses: nais/docker-build-push@v0
        id: kafka-docker-build-push
        with:
          dockerfile: ./dinesykmeldte-kafka/Dockerfile
          docker_context: dinesykmeldte-kafka
          image_suffix: dinesykmeldte-kafka
          team: teamsykmelding
          identity_provider: ${{ secrets.NAIS_WORKLOAD_IDENTITY_PROVIDER }}
          project_id: ${{ vars.NAIS_MANAGEMENT_PROJECT_ID }}

    outputs:
      stream-image: ${{ steps.kafka-docker-build-push.outputs.image }}
  deploy-dev:
    name: Deploy to dev
    runs-on: ubuntu-latest
    needs: [ build-kafka, snyk-test ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Deploy network policy to dev-gcp
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CLUSTER: dev-gcp
          TEAM: teamsykmelding
          RESOURCE:  dinesykmeldte-kafka/network-policy-dev.yaml
          PRINT_PAYLOAD: true
      - name: Deploy to dev
        uses: nais/deploy/actions/deploy@master
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: dinesykmeldte-kafka/naiserator-dev.yaml
          VAR: stream-image=${{ needs.build-kafka.outputs.stream-image }}